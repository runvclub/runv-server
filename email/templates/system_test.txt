runv.club — test email

This is an automated message to verify the mail subsystem.

Administrator: {admin_email}
Configured sender: {default_from}
Registered domain / host: {host}
API base URL (if Mailgun): {api_base_url}
UNIX timestamp: {timestamp}

If you received this message, the configured backend (Mailgun API or legacy SMTP) and the library are operational.
